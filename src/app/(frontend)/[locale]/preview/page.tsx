import { HomepageSections } from '@/components/homepage/sections'

export default function PreviewHomepage() {
  return (
    <HomepageSections
      homepage={null}
      capabilities={[]}
      caseStudies={[]}
      clients={[]}
      featuredInsights={[]}
    />
  )
}
